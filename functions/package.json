{
  "name": "violingo-functions",
  "private": true,
  "main": "dist/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/index.js --external:firebase-functions --external:firebase-admin"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5"
  }
}
