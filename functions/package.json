{
  "name": "violingo-functions",
  "private": true,
  "main": "dist/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js --external:firebase-functions --external:firebase-admin"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5"
  }
}
